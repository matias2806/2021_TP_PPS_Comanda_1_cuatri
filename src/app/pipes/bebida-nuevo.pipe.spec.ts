import { BebidaNuevoPipe } from './bebida-nuevo.pipe';

describe('BebidaNuevoPipe', () => {
  it('create an instance', () => {
    const pipe = new BebidaNuevoPipe();
    expect(pipe).toBeTruthy();
  });
});
