import { ComidaNuevoPipe } from './comida-nuevo.pipe';

describe('ComidaNuevoPipe', () => {
  it('create an instance', () => {
    const pipe = new ComidaNuevoPipe();
    expect(pipe).toBeTruthy();
  });
});
